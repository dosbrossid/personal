// Next.js includes a small legacy-browser polyfill module by default.
// The public blog targets modern browsers, so this alias keeps Lighthouse from
// counting unused legacy JavaScript on modern mobile audits.
export {};
